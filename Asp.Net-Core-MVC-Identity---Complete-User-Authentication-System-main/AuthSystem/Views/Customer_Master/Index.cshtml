@model IEnumerable<eros.Models.Customer_Master>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    string role = Context.Request.HttpContext.Session.GetString("Role");
    var jsonData = Context.Request.HttpContext.Session.GetString("MenuMaster");
}
@* @if (!string.IsNullOrEmpty(role))
{
    @if (role == "ADMIN" || role == "HOSITE" || role == "WAREHOUSE")
    {
        <div class="container-field">
            <div class="mt-3">
                <div class="card mb-3">
                    <div class="card-header bg-dark text-white">
                        <div class="d-flex align-items-baseline">
                            <h5 class="mx-auto font-weight-bolder">Customer Master</h5>
                            <a asp-action="Create" class="btn btn-success text-white"><i class="fas fa-plus-square"></i> Add</a>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-bordered table-hover" id="dataTable" width="100%" cellspacing="0">
                                <thead class="thead-light">
                                    <tr>
                                        <th>Id</th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.customername)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.contactperson)
                                        </th>
                                        <th style="display:none">
                                            @Html.DisplayNameFor(model => model.address)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.city)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.contactno)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.emailid)
                                        </th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @{
                                        int counter = 1;
                                    }
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>
                                                @counter
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.customername)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.contactperson)
                                            </td>
                                            <td style="display:none">
                                                @Html.DisplayFor(modelItem => item.address)
                                            </td>
                                            <td  style="width:100px">
                                                @Html.DisplayFor(modelItem => item.city)
                                            </td>
                                            <td style="width:100px">
                                                @Html.DisplayFor(modelItem => item.contactno)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.emailid)
                                            </td>
                                            <td>
                                                <a asp-action="Edit" asp-route-id="@item.customer_id" class="btn btn-success ">
                                                    <i class="fas fa-pencil-alt"></i>
                                                </a>
                                                <a asp-action="Details" asp-route-id="@item.customer_id" class="btn bg-warning">
                                                    <i style="color:white" class="fa fa-eye" aria-hidden="true"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@item.customer_id" class="btn btn-danger">
                                                    <i class="fas fa-trash-alt"></i>
                                                </a>
                                            </td>
                                        </tr>
                                        counter++;
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    }
        else
    {
        <div class="container">
            <div class="alert alert-danger alert-dismissible fade show">
                <h6 class="alert-heading">
                    <i class="bi-exclamation-octagon-fill"></i>
                    No Access !!
                </h6>
                <p>
                    You do not have access to this page. Please contact your administrator for assistance.
                </p>
                <hr>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        </div>
    }

}
 *@

@if (!string.IsNullOrEmpty(role))
{
    @if (role == "ADMIN" || role == "HOSITE" || role == "WAREHOUSE")
    {
        <div class="container-field">
            <div class="mt-3 text-center">
                <!-- Centering added here -->
                <div class="card mb-3 mx-auto" >
                    <!-- Center card and limit width -->
                    <div class="card-header bg-dark text-white">
                        <div class="d-flex justify-content-between align-items-baseline">
                            <h5 class="mx-auto font-weight-bolder">Customer Master</h5>
                            <a asp-action="Create" class="btn btn-success text-white"><i class="fas fa-plus-square"></i> Add</a>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-bordered table-hover" id="dataTable" width="100%" cellspacing="0">
                                <thead class="thead-light text-center">
                                    <!-- Center table headers -->
                                    <tr>
                                        <th>Sr.No.</th>
                                        <th style="width:150px;">@Html.DisplayNameFor(model => model.customername)</th>
                                        <th style="width:150px;">@Html.DisplayNameFor(model => model.contactperson)</th>
                                        <th style="display:none">@Html.DisplayNameFor(model => model.address)</th>
                                        <th>@Html.DisplayNameFor(model => model.city)</th>
                                        <th style=" width:120px;">Contact No.</th>
                                        <th>@Html.DisplayNameFor(model => model.emailid)</th>
                                        <th style="width:150px;">Action</th>
                                    </tr>
                                </thead>
                                <tbody class="text-center">
                                    <!-- Center table body -->
                                    @{
                                        int counter = 1;
                                    }
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@counter</td>
                                            <td>@Html.DisplayFor(modelItem => item.customername)</td>
                                            <td>@Html.DisplayFor(modelItem => item.contactperson)</td>
                                            <td style="display:none">@Html.DisplayFor(modelItem => item.address)</td>
                                            <td style="width:100px">@Html.DisplayFor(modelItem => item.city)</td>
                                            <td style="width:100px">@Html.DisplayFor(modelItem => item.contactno)</td>
                                            <td>@Html.DisplayFor(modelItem => item.emailid)</td>
                                            <td>
                                                <a asp-action="Edit" asp-route-id="@item.customer_id" class="btn btn-success">
                                                    <i class="fas fa-pencil-alt"></i>
                                                </a>
                                                <a asp-action="Details" asp-route-id="@item.customer_id" class="btn bg-warning">
                                                    <i style="color:white" class="fa fa-eye" aria-hidden="true"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@item.customer_id" class="btn btn-danger">
                                                    <i class="fas fa-trash-alt"></i>
                                                </a>
                                            </td>
                                        </tr>
                                        counter++;
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="container text-center">
            <!-- Centering the alert -->
            <div class="alert alert-danger alert-dismissible fade show mx-auto" style="max-width: 400px;">
                <h6 class="alert-heading">
                    <i class="bi-exclamation-octagon-fill"></i>
                    No Access !!
                </h6>
                <p>
                    You do not have access to this page. Please contact your administrator for assistance.
                </p>
                <hr>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        </div>
    }
}

 
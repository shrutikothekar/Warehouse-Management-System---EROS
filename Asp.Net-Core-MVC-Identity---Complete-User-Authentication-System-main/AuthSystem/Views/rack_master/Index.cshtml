@model IEnumerable<eros.Models.rack_master>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<div class="mt-3">
    <div class="card mb-3">
        <div class="card-header bg-dark text-white">
            <div class="d-flex align-items-baseline">
                <h5 class="mx-auto font-weight-bolder">Rack Master</h5>
                <a asp-action="Create" class="btn btn-success text-white"><i class="fas fa-plus-square"></i> Add</a>
            </div>
        </div>*@
<div class="container">
    <div class="mt-3">
        <div class="card mb-3">
            <div class="card-header bg-dark text-white">
                <div class="d-flex align-items-baseline">
                    <h5 class="mx-auto font-weight-bolder">Rack Master</h5>
                    <a asp-action="Create" class="btn btn-success text-white"><i class="fas fa-plus-square"></i> Add</a>
                </div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover" id="dataTable" width="100%" cellspacing="0">
                        <thead class="thead-light text-center">
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.rackname)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.shelves)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.bin)
                                </th>

                                <th style="width:150px;">Action</th>
                            </tr>
                        </thead>
                        <tbody class=" text-center">
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.rackname)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.shelves)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.bin)
                                    </td>


                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.id" class="btn btn-success ">
                                            <i class="fas fa-pencil-alt"></i>
                                        </a>
                                        <a asp-action="Details" asp-route-id="@item.id" class="btn bg-warning">
                                            <i style="color:white" class="fa fa-eye" aria-hidden="true"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.id" class="btn btn-danger">
                                            <i class="fas fa-trash-alt"></i>
                                        </a>
                                        @*                                     <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.id">Edit</a>&nbsp;
                                    <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.id">Details</a> &nbsp;
                                    <a class="btn btn-primary" asp-action="Delete" asp-route-id="@item.id">Delete</a>
                                    *@
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
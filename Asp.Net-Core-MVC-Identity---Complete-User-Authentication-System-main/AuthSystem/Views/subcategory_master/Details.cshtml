@model eros.Models.subcategory_master

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
    string role = Context.Request.HttpContext.Session.GetString("Role");
    var jsonData = Context.Request.HttpContext.Session.GetString("MenuMaster");
}
@if (!string.IsNullOrEmpty(role))
{
    @if (role == "ADMIN" || role == "HOSITE" || role == "WAREHOUSE")
    {
        <div class="container">
            <div class="mt-3">
                <div class="card mb-3">
                    <div class="card-header bg-dark text-white">
                        <div class="d-flex align-items-baseline">
                            <h5 class="mx-auto font-weight-bolder">Sub-Category Master Details</h5>
                            <a asp-action="Index" class="btn btn-success text-white"><i class="fa fa-reply"></i> Back</a>
                        </div>
                    </div>
                    <form asp-action="Details" id="CodeSbyAnizForm">
                        <div>
                            <dl class="row mx-lg-5 mt-lg-4">
                                <dt class="col-sm-3">
                                    @Html.DisplayNameFor(model => model.categoryname)
                                </dt>
                                <dd class="col-sm-9">
                                    @Html.DisplayFor(model => model.categoryname)
                                </dd>
                                <dt class="col-sm-3">
                                    @Html.DisplayNameFor(model => model.subcategory_name)
                                </dt>
                                <dd class="col-sm-9">
                                    @Html.DisplayFor(model => model.subcategory_name)
                                </dd>
                            </dl>
                        </div>
                    </form>

                    @*  <div class="row mt-lg-3 mb-lg-3 mb-3">
            <div class="col-sm-5 mx-lg-5 mx-sm-5 mx-md-5">
            <a asp-action="Edit" asp-route-id="@Model?.subcategory_id" class="btn btn-primary col-md-12 col-sm-12">Edit</a>
            </div>
            <div class="col-sm-5 mx-lg-4 mx-sm-4 mx-md-4">
            <a asp-action="Index" class="btn btn-danger col-md-12 col-sm-12">Back to List</a>
            </div>
            </div> *@
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="container">
            <div class="alert alert-danger alert-dismissible fade show">
                <h6 class="alert-heading">
                    <i class="bi-exclamation-octagon-fill"></i>
                    No Access !!
                </h6>
                <p>
                    You do not have access to this page. Please contact your administrator for assistance.
                </p>
                <hr>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        </div>
    }
}

